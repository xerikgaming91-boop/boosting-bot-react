{
  "name": "client",
  "private": true,
  "version": "1.0.1",
  "type": "module",
  "description": "React UI wird über Express (ein Port) bereitgestellt. Nicht separat starten.",
  "scripts": {
    "dev": "node -e \"console.error('❌ Bitte im Projekt-Root npm run dev starten – alles läuft auf http://localhost:3000'); process.exit(1)\"",
    "build": "node -e \"console.error('❌ Build bitte im Projekt-Root ausführen: npm run build'); process.exit(1)\"",
    "preview": "node -e \"console.error('❌ Vorschau nicht nötig – Express dient das Build aus.'); process.exit(1)\""
  },
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.6",
    "tailwindcss": "^4.1.12"
  }
}
